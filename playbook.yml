---
- hosts: web
  sudo: true
  vars_files:
    - vars/all.yml
  roles:
    - server
#    - vagrant_local
    - nginx
#    - rabbitmq
    - php
    - composer
    - app
- hosts: db
  sudo: true
  vars_files:
    - vars/all.yml
  roles:
    - mysql
    - elasticsearch
